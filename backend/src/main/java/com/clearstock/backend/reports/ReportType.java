package com.clearstock.backend.reports;

public enum ReportType {
    /** A problem with a listing itself (misleading, prohibited, wrong price…). */
    LISTING,
    /** Behaviour of another person — a seller or a buyer. */
    USER
}
